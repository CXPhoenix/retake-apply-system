"""校園重補修課程登記系統 (retake_apply) 主應用程式套件。

這是 `retake_apply` 應用程式的核心套件，包含了所有的模型 (models)、
頁面 (pages)、狀態 (states)、元件 (components)、組態 (configs)
以及工具函式 (utils) 等子模組。
"""

# 此檔案主要用於將 'retake_apply' 目錄標記為一個 Python 套件。
# 根據需要，未來可以在此處進行套件級別的匯出或初始化。
